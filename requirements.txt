# Streamlit app core
streamlit>=1.33,<1.40

# Mic recorder widget used in Voice mode
streamlit-mic-recorder>=0.0.8,<0.0.10

# Whisper (CPU) + deps
faster-whisper>=1.0.0,<1.2
ctranslate2>=4.3,<5
tokenizers>=0.15,<0.16
sentencepiece>=0.1.99,<0.2

# Audio decoding / I/O
soundfile>=0.12.1,<0.14
av>=12.0.0,<13

# ML + utils
numpy>=1.26,<2
scikit-learn>=1.5,<1.6
xgboost>=2.0,<3
joblib>=1.3,<2
