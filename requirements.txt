# Streamlit UI
streamlit==1.33.0
streamlit-mic-recorder==0.0.8

# Core scientific stack (Py3.13 → NumPy 2.x + sklearn >=1.6)
numpy>=2.1,<3
scikit-learn>=1.6,<1.7
xgboost>=2.1,<3
joblib>=1.3,<2

# Whisper (CPU) stack (versions that have Py3.13 wheels)
faster-whisper>=1.0.3
ctranslate2==4.6.0
tokenizers>=0.15,<0.16
sentencepiece>=0.1.99,<0.2

# Audio I/O (use SoundFile; don't pin av — let resolver decide or skip it)
soundfile>=0.12.1,<0.14
