# UI
streamlit>=1.41,<2
streamlit-mic-recorder==0.0.8

# Core scientific stack (Py3.13 needs NumPy 2.x; sklearn 1.6 supports it)
numpy>=2.1,<3
scikit-learn>=1.6,<1.7
xgboost>=2.1,<3
joblib>=1.3,<2

# Whisper (CPU)
faster-whisper>=1.2.0
ctranslate2==4.6.0
tokenizers>=0.15,<0.16
sentencepiece==0.1.99

# Audio I/O (we avoid forcing PyAV)
soundfile>=0.12.1,<0.14
